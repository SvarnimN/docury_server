### Docury
Know your docs

### RAG elements
- FastEmbed for embeddings.
- FAISS for vector store.
- Gemini for LLM.

### Setup
- Install `uv` with `pip install uv`
- Run `uv sync`
- Create an `.env` file. Add `GOOGLE_API_KEY` and `REDIS_URL`
- Run `uv run fastapi dev`
